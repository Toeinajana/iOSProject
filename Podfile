# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DreamWorld' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for DreamWorld

  target 'DreamWorldTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DreamWorldUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

pod 'FBSDKLoginKit'